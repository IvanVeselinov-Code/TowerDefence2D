

if (global.pausePhase and global.moveable and pickup >= 100){
		//var particle2 = part_system_create(ParticleSystem3);
		//part_system_position(particle2, global.selected_tower.x, global.selected_tower.y);
	if (!mouse_check_button_released(mb_left) and released = 1){
		x = mouse_x;
		y = mouse_y;
		move_snap(32, 32);
	}
}

